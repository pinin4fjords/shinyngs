Compare the number and direction of differential features across contrasts.

The bars use the same matrix, row selection, contrasts, and statistical thresholds selected in the controls. Features with positive fold changes are shown to the right and features with negative fold changes to the left.

Use the summary table for exact counts and CSV export. This page describes the size and directional balance of each differential result set. Use **Differential set intersection** to examine overlap between those sets.
