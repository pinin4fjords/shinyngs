% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap.R
\name{plotly_pca_metadata_heatmap}
\alias{plotly_pca_metadata_heatmap}
\title{Make a PCA-vs-metadata association heatmap with \code{heatmaply()}}
\usage{
plotly_pca_metadata_heatmap(
  pca_coords,
  pcameta,
  fraction_explained,
  cluster_rows = TRUE,
  n_components = 10,
  plot_height = NULL,
  ...
)
}
\arguments{
\item{pca_coords}{Data frame of PCA coordinates, with samples by row and
components by column (e.g. the \code{x} element of \code{\link{runPCA}}'s
\code{prcomp} result).}

\item{pcameta}{Data frame of sample metadata with sample identifiers by row
and variables by column.}

\item{fraction_explained}{Numeric vector containing the percent contribution
to variance of each component (e.g. from
\code{\link{calculatePCAFractionExplained}}).}

\item{cluster_rows}{Cluster variables (rows) by their p value profile
across components?}

\item{n_components}{Number of leading components to test, passed through
to \code{\link{anova_pca_metadata}}.}

\item{plot_height}{The total rendered height of the plot in pixels, passed
through to \code{\link{interactiveHeatmap}}. Defaults to a height scaled
to the number of variables.}

\item{...}{Additional arguments passed to \code{\link{interactiveHeatmap}}}
}
\value{
output A plotly htmlwidget as produced by \code{\link{interactiveHeatmap}}
}
\description{
Runs \code{\link{anova_pca_metadata}} on the supplied PCA coordinates and
sample metadata, then renders the resulting p value matrix with
\code{\link{interactiveHeatmap}}: -log10(p) sets the cell color, the raw p
values are shown on hover, and variables (rows) with a single, uninformative
value across all shown cells are dropped when \code{cluster_rows} is TRUE.
}
\examples{
pcameta <- data.frame(
  row.names = paste0("sample", 1:6),
  treatment = rep(c("control", "treated"), each = 3),
  batch = rep(c("a", "b"), 3)
)
pca_coords <- matrix(rnorm(6 * 4), nrow = 6, dimnames = list(rownames(pcameta), paste0("PC", 1:4)))

plotly_pca_metadata_heatmap(pca_coords, pcameta, fraction_explained = c(45, 25, 20, 10))

}
