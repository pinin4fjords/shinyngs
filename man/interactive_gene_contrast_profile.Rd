% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genecontrastprofile.R
\name{interactive_gene_contrast_profile}
\alias{interactive_gene_contrast_profile}
\title{Plot one feature's fold change across contrasts}
\usage{
interactive_gene_contrast_profile(
  contrast_table,
  q_threshold = 0.05,
  title = "Contrast profile"
)
}
\arguments{
\item{contrast_table}{Data frame containing a \code{Fold change} column
and either a \code{Contrast} column or \code{Variable},
\code{Condition 1}, and \code{Condition 2} columns.}

\item{q_threshold}{Numeric q-value threshold used to distinguish marker
symbols.}

\item{title}{Plot title.}
}
\value{
A plotly object.
}
\description{
Builds an interactive dot plot from a gene-page contrast table. Fold changes
use shinyngs' signed linear representation and are shown on a signed log2
scale. When q values are present, filled markers meet \code{q_threshold},
open markers do not, and crosses have no q value.
}
\examples{
interactive_gene_contrast_profile(data.frame(
  Variable = c("condition", "condition"),
  `Condition 1` = c("control", "control"),
  `Condition 2` = c("treated_a", "treated_b"),
  `Fold change` = c(2, -4),
  `q value` = c(0.01, 0.2),
  check.names = FALSE
))
}
