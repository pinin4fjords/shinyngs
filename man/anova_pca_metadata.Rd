% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap.R
\name{anova_pca_metadata}
\alias{anova_pca_metadata}
\title{Generate a matrix of anova values for associating principal components with
categorical covariates.}
\usage{
anova_pca_metadata(pca_coords, pcameta, fraction_explained, n_components = 10)
}
\arguments{
\item{pca_coords}{Data frame of PCA coordinates, with samples by row and
components by column.}

\item{pcameta}{Data frame of sample metadata with sample identifiers by row
and variables by column.}

\item{fraction_explained}{Numeric vector containing the percent contribution
to variance of each component}

\item{n_components}{Number of leading components to test. Clamped to the
number actually available in \code{pca_coords} if that's fewer.}
}
\value{
output A numeric matrix of p values
}
\description{
Generate a matrix of anova values for associating principal components with
categorical covariates.
}
\examples{
mat <- matrix(rnorm(90), nrow = 15, ncol = 6,
  dimnames = list(paste0("gene", 1:15), paste0("s", 1:6)))
pca <- compile_pca_data(mat)
pcameta <- data.frame(
  condition = rep(c("treated", "control"), each = 3),
  batch = rep(c("A", "B", "C"), 2),
  row.names = colnames(mat)
)
anova_pca_metadata(pca$coords, pcameta, pca$percentVar)

}
