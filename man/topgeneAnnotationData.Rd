% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topgeneboxplot.R
\name{topgeneAnnotationData}
\alias{topgeneAnnotationData}
\title{Build the per-facet annotation data frame used by
\code{\link{ggplot_topgene_boxplots}}}
\usage{
topgeneAnnotationData(annotations, genes)
}
\arguments{
\item{annotations}{Optional named character vector keyed by the values in
\code{genes}, rendered as a per-facet annotation (e.g. a q value string)}

\item{genes}{Character vector of row names of \code{assay} to facet on, in
the order facets should appear. Also used to look up values in
\code{assay}, so must match its row names even when \code{labels} is
supplied.}
}
\value{
A data frame with columns \code{gene} (factor, levels = \code{genes})
and \code{label}, or \code{NULL} if no annotations were supplied/matched
}
\description{
Build the per-facet annotation data frame used by
\code{\link{ggplot_topgene_boxplots}}
}
\keyword{internal}
