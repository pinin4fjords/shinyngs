# Extract the percent variance from a PCA analysis

Extract the percent variance from a PCA analysis

## Usage

``` r
calculatePCAFractionExplained(pca)
```

## Arguments

- pca:

  An output from `prcomp`

## Value

output vector of percentages

## Examples

``` r
calculatePCAFractionExplained(pca)
#> Error in calculatePCAFractionExplained(pca): could not find function "calculatePCAFractionExplained"
```
