# Build a single clickable summary tile

Build a single clickable summary tile

## Usage

``` r
summaryTileTag(ns, spec)
```

## Arguments

- ns:

  Namespace function for the calling module

- spec:

  A tile spec list (see `summaryTileSpecs`)

## Value

A tag representing the tile, wired as a Shiny action input
