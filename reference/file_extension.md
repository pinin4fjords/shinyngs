# Extract the extension of a file

Extract the extension of a file

## Usage

``` r
file_extension(file)
```

## Arguments

- file:

  Input file path

## Value

extension Extension like 'csv' or 'tsv'

## Examples

``` r
file_extension("my_metadata.tsv")
#> [1] "tsv"
```
