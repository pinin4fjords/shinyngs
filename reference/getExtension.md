# Extract the extension of a file

Extract the extension of a file

## Usage

``` r
getExtension(file)
```

## Arguments

- file:

  Input file path

## Value

extension Extension like 'csv' or 'tsv'

## Examples

``` r
getExtension("my_metadata.tsv")
#> [1] "tsv"
```
