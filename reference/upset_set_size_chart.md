# Make the bar chart illustrating set sizes

Make the bar chart illustrating set sizes

## Usage

``` r
upset_set_size_chart(sets)
```

## Arguments

- sets:

  A named list of character vectors, one per set

## Value

output A plotly htmlwidget
