# Remove random effects from a model formula

Remove random effects from a model formula

## Usage

``` r
fixedEffectsFormula(formula_string)
```

## Arguments

- formula_string:

  Formula string that may contain random effects

## Value

output Fixed-effects-only formula
