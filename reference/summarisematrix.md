# The server function of the summarisematrix module

This module provides a form element and associated get function for
defining how a summary statistic is calculated (probably by mean).

## Usage

``` r
summarisematrix(id)
```

## Arguments

- id:

  Module namespace

## Examples

``` r
summarisematrix()
#> Error in summarisematrix(): could not find function "summarisematrix"
```
